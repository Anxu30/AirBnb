<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>AIR BNB</title>
  <script src="https://cdn.tailwindcss.com">
  </script>
</head>
<body class="min-h-screen bg-gradient-to-br from-cyan-700 via-purple-700 to-pink-500 flex flex-col">
  <div>
    <%-include('../partials/nav.ejs')%>
  </div>
  <div class="flex flex-col items-center justify-center min-h-[80vh] px-4">
    <div class="glass p-10 md:p-14 flex flex-col items-center gap-8 w-full max-w-lg mt-10 shadow-2xl animate-fade-in-up">
      <h1 class="text-4xl md:text-5xl font-extrabold text-transparent bg-clip-text bg-gradient-to-r from-yellow-400 via-pink-500 to-purple-600 animate-pulse drop-shadow-lg text-center mb-2">
        <% if(editing==true){ %>
          Edit Your Home
        <% } else { %>
          Add a New Home
        <% } %>
      </h1>
      <% if(editing==true){ %>
        <form class="w-full flex flex-col gap-6" action="/host/editHome/<%=home._id%>?editing=true" method="post" autocomplete="off">
          <div class="flex flex-col gap-4">
            <label for="home" class="text-gray-200 font-semibold pl-1">🏠 Home Name</label>
            <input class="w-full px-4 py-3 rounded-lg bg-gray-800/80 text-gray-100 placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-pink-400 transition-all font-medium shadow-inner"
              placeholder="Home Name" type="text" name="homes" id="home" value="<%=home.HomeName%>" required>
          </div>
          <div class="flex flex-col gap-4">
            <label for="location" class="text-gray-200 font-semibold pl-1">📍 Location/City</label>
            <input class="w-full px-4 py-3 rounded-lg bg-gray-800/80 text-gray-100 placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-pink-400 transition-all font-medium shadow-inner"
              placeholder="Location/City" type="text" name="locations" id="location" value="<%=home.Location%>" required>
          </div>
          <div class="flex flex-col gap-4">
            <label for="price" class="text-gray-200 font-semibold pl-1">💲 Price/Night</label>
            <input class="w-full px-4 py-3 rounded-lg bg-gray-800/80 text-gray-100 placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-pink-400 transition-all font-medium shadow-inner"
              placeholder="Price per Night" type="number" min="0" name="prices" id="price" value="<%=home.Price%>" required>
          </div>
          <div class="flex flex-col gap-4">
            <label for="rate" class="text-gray-200 font-semibold pl-1">⭐ Rating (out of 5)</label>
            <input class="w-full px-4 py-3 rounded-lg bg-gray-800/80 text-gray-100 placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-pink-400 transition-all font-medium shadow-inner"
              placeholder="Rating (1-5)" type="number" min="1" max="5" step="0.1" name="rating" id="rate" value="<%=home.Rating%>" required>
          </div>
          <div class="flex flex-col gap-4">
            <label for="url" class="text-gray-200 font-semibold pl-1">📸 Photo URL</label>
            <input class="w-full px-4 py-3 rounded-lg bg-gray-800/80 text-gray-100 placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-pink-400 transition-all font-medium shadow-inner"
              placeholder="Photo URL" type="url" name="homeurl" id="url" value="<%=home.PhotoURL%>" required>
          </div>
          <div class="flex flex-col gap-4">
            <label for="desc" class="text-gray-200 font-semibold pl-1">📝 Description</label>
            <textarea class="w-full px-4 py-3 rounded-lg bg-gray-800/80 text-gray-100 placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-pink-400 transition-all font-medium shadow-inner resize-none"
              name="description" id="desc" placeholder="Description" rows="3" required><%=home.Description%></textarea>
          </div>
          <input type="hidden" name="Id" value="<%=home._id%>">
          <button
            type="submit"
            class="w-full py-3 mt-2 rounded-full bg-gradient-to-r from-yellow-400 via-pink-500 to-purple-600 text-white font-bold text-lg shadow-lg hover:scale-105 hover:shadow-2xl transition-all duration-300"
          >
            Edit Home
          </button>
        </form>
      <% } else { %>
        <form class="w-full flex flex-col gap-6" action="/host/editHome" method="post" autocomplete="off">
          <div class="flex flex-col gap-4">
            <label for="home" class="text-gray-200 font-semibold pl-1">🏠 Home Name</label>
            <input class="w-full px-4 py-3 rounded-lg bg-gray-800/80 text-gray-100 placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-pink-400 transition-all font-medium shadow-inner"
              placeholder="Home Name" type="text" name="homes" id="home" required>
          </div>
          <div class="flex flex-col gap-4">
            <label for="location" class="text-gray-200 font-semibold pl-1">📍 Location/City</label>
            <input class="w-full px-4 py-3 rounded-lg bg-gray-800/80 text-gray-100 placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-pink-400 transition-all font-medium shadow-inner"
              placeholder="Location/City" type="text" name="locations" id="location" required>
          </div>
          <div class="flex flex-col gap-4">
            <label for="price" class="text-gray-200 font-semibold pl-1">💲 Price/Night</label>
            <input class="w-full px-4 py-3 rounded-lg bg-gray-800/80 text-gray-100 placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-pink-400 transition-all font-medium shadow-inner"
              placeholder="Price per Night" type="number" min="0" name="prices" id="price" required>
          </div>
          <div class="flex flex-col gap-4">
            <label for="rate" class="text-gray-200 font-semibold pl-1">⭐ Rating (out of 5)</label>
            <input class="w-full px-4 py-3 rounded-lg bg-gray-800/80 text-gray-100 placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-pink-400 transition-all font-medium shadow-inner"
              placeholder="Rating (1-5)" type="number" min="1" max="5" step="0.1" name="rating" id="rate" required>
          </div>
          <div class="flex flex-col gap-4">
            <label for="url" class="text-gray-200 font-semibold pl-1">📸 Photo URL</label>
            <input class="w-full px-4 py-3 rounded-lg bg-gray-800/80 text-gray-100 placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-pink-400 transition-all font-medium shadow-inner"
              placeholder="Photo URL" type="url" name="homeurl" id="url" required>
          </div>
          <div class="flex flex-col gap-4">
            <label for="desc" class="text-gray-200 font-semibold pl-1">📝 Description</label>
            <textarea class="w-full px-4 py-3 rounded-lg bg-gray-800/80 text-gray-100 placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-pink-400 transition-all font-medium shadow-inner resize-none"
              name="description" id="desc" placeholder="Description" rows="3" required></textarea>
          </div>
          <button
            type="submit"
            class="w-full py-3 mt-2 rounded-full bg-gradient-to-r from-yellow-400 via-pink-500 to-purple-600 text-white font-bold text-lg shadow-lg hover:scale-105 hover:shadow-2xl transition-all duration-300"
          >
            Add Home
          </button>
        </form>
      <% } %>
    </div>
  </div>
  <style>
    @keyframes fadeInUp {
      0% { opacity: 0; transform: translateY(40px);}
      100% { opacity: 1; transform: translateY(0);}
    }
    .animate-fade-in-up {
      animation: fadeInUp 1.2s cubic-bezier(0.4,0,0.2,1) both;
    }
    .glass {
      background: rgba(255,255,255,0.18);
      backdrop-filter: blur(10px);
      border-radius: 1.5rem;
      box-shadow: 0 8px 32px 0 rgba(31, 38, 135, 0.25);
      border: 1px solid rgba(255,255,255,0.18);
    }
  </style>
</body>
</html>