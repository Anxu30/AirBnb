<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>AIR BNB</title>
  <script src="https://cdn.tailwindcss.com">
  </script>
</head>
<body class="bg-gradient-to-br from-blue-200 via-purple-200 to-pink-200 min-h-screen flex flex-col">
  <%- include('../partials/nav.ejs') %>
  <div class="flex flex-1 items-center justify-center">
    <div class="bg-white rounded-3xl shadow-2xl p-10 max-w-md w-full text-center animate-fade-in-up">
      <div class="flex justify-center mb-6">
        <svg class="w-16 h-16 text-green-400" fill="none" stroke="currentColor" stroke-width="2.5" viewBox="0 0 24 24">
          <circle cx="12" cy="12" r="10" stroke="currentColor" stroke-width="2.5" fill="none"/>
          <path stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" d="M8 12l2.5 2.5L16 9"/>
        </svg>
      </div>
      <h1 class="text-2xl font-extrabold text-gray-800 mb-2 animate-bounce">
        <%= editing ? "Home Updated Successfully!" : "Home Registered Successfully!" %>
      </h1>
      <p class="text-gray-600 mb-6">
        <%= editing ? "Your home details have been updated. Thank you for keeping your listing up to date!" : "Your home has been added to our platform. Thank you for sharing your space!" %>
      </p>
      <a href="/hosthomes" class="inline-block px-6 py-2 bg-gradient-to-r from-purple-400 to-pink-400 text-white rounded-full font-semibold shadow hover:from-pink-400 hover:to-purple-400 transition">
        View My Homes
      </a>
    </div>
  </div>
  <style>
    @keyframes fade-in-up {
      0% {
        opacity: 0;
        transform: translateY(40px);
      }
      100% {
        opacity: 1;
        transform: translateY(0);
      }
    }
    .animate-fade-in-up {
      animation: fade-in-up 0.8s cubic-bezier(0.4, 0, 0.2, 1) both;
    }
  </style>
</body>
</html>