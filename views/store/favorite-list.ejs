<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>AIR BNB</title>
  <script src="https://cdn.tailwindcss.com">
  </script>
</head>
<body class="min-h-screen bg-gradient-to-br from-cyan-700 via-purple-700 to-pink-500 flex flex-col">
  <div>
    <%- include('../partials/nav.ejs') %>
    <div class="flex flex-col items-center justify-center mt-10 mb-6">
      <h1 class="text-4xl md:text-5xl font-extrabold text-transparent bg-clip-text bg-gradient-to-r from-yellow-400 via-pink-500 to-purple-600 animate-pulse drop-shadow-lg text-center mb-2">
        Your Favorite Homes
      </h1>
      <p class="text-lg text-gray-200 mb-8">A curated list of homes you love!</p>
    </div>
    <div class="flex flex-wrap justify-center gap-8 px-4 pb-12">
      <% if (data && data.length) { %>
        <% data.forEach((h) => { %>
          <div class="relative bg-white/80 backdrop-blur-md rounded-3xl shadow-2xl p-6 w-80 flex flex-col items-center transition-transform duration-500 hover:scale-105 hover:shadow-pink-400/40 group">
            <div class="w-60 h-60 mb-4 rounded-2xl overflow-hidden shadow-lg border-4 border-pink-200 group-hover:border-yellow-300 transition-all duration-300">
              <img 
                src="<%= h.PhotoURL %>" 
                alt="HomeImage"
                class="object-cover w-full h-full transition-transform duration-700 group-hover:scale-110 group-hover:rotate-2"
              >
            </div>
            <h2 class="text-2xl font-bold text-gray-800 mb-1 flex items-center gap-2">
              <span>🏠</span><%= h.HomeName %>
            </h2>
            <p class="text-gray-600 font-medium mb-1 flex items-center gap-2">
              <span>📍</span><%= h.Location %>
            </p>
            <p class="text-pink-600 font-semibold text-lg mb-1 flex items-center gap-2">
              <span>💲</span><%= h.Price %>
            </p>
            <p class="flex items-center gap-2 text-yellow-500 font-bold text-lg mb-4 animate-bounce">
              <span>⭐</span><%= h.Rating %>
            </p>
            <a 
              href="/unfav/<%= h._id %>" 
              class="absolute top-4 right-4 bg-gradient-to-r from-pink-500 to-yellow-400 text-white rounded-full p-3 shadow-lg hover:scale-110 hover:from-yellow-400 hover:to-pink-500 transition-all duration-300 focus:outline-none focus:ring-2 focus:ring-pink-400"
              title="Remove from Favorites"
              type="button"
            >
              <span class="text-2xl">💝</span>
            </a>
          </div>
        <% }) %>
      <% } else { %>
        <div class="w-full flex flex-col items-center mt-16">
          <svg class="w-24 h-24 text-pink-300 mb-4 animate-bounce" fill="none" stroke="currentColor" stroke-width="2.5" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" d="M12 21C12 21 4 13.5 4 8.5C4 5.42 6.42 3 9.5 3C11.24 3 12.91 3.81 14 5.08C15.09 3.81 16.76 3 18.5 3C21.58 3 24 5.42 24 8.5C24 13.5 16 21 16 21H12Z"/>
          </svg>
          <h3 class="text-2xl font-bold text-gray-200 mb-2">No favorites yet!</h3>
          <p class="text-gray-300">Start adding homes to your favorites to see them here.</p>
        </div>
      <% } %>
    </div>
  </div>
</body>
</html>
