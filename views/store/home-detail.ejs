<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>AIR BNB</title>
  <script src="https://cdn.tailwindcss.com">
  </script>
</head>
<body class="min-h-screen bg-gradient-to-br from-cyan-700 via-purple-700 to-pink-500 flex flex-col">
  <div>
    <%- include('../partials/nav.ejs') %>
    <div class="flex flex-col items-center justify-center mt-10 mb-6">
      <h1 class="text-4xl md:text-5xl font-extrabold text-transparent bg-clip-text bg-gradient-to-r from-yellow-400 via-pink-500 to-purple-600 animate-pulse drop-shadow-lg text-center mb-2">
        Home Details
      </h1>
      <p class="text-lg text-gray-200 mb-8">Discover all about this beautiful home!</p>
    </div>
    <div class="flex justify-center items-center px-4 pb-12">
      <div class="relative bg-white/80 backdrop-blur-md rounded-3xl shadow-2xl p-8 w-full max-w-xl flex flex-col items-center transition-transform duration-500 hover:scale-105 hover:shadow-pink-400/40 group">
        <div class="w-64 h-64 mb-6 rounded-2xl overflow-hidden shadow-lg border-4 border-pink-200 group-hover:border-yellow-300 transition-all duration-300">
          <img 
            src="<%= home.PhotoURL %>" 
            alt="HomeImage"
            class="object-cover w-full h-full transition-transform duration-700 group-hover:scale-110 group-hover:rotate-2"
          >
        </div>
        <h2 class="text-3xl font-bold text-gray-800 mb-2 flex items-center gap-2">
          <span>🏠</span><%= home.HomeName %>
        </h2>
        <p class="text-gray-600 font-medium mb-2 flex items-center gap-2">
          <span>📍</span><%= home.Location %>
        </p>
        <p class="text-gray-700 mb-4 text-center px-2">
          <span class="font-semibold">🧾 Description:</span> <%= home.Description %> <%=home.Location%>
        </p>
        <div class="flex flex-row gap-6 mb-4">
          <p class="text-pink-600 font-semibold text-lg flex items-center gap-2">
            <span>💲</span><%= home.Price %>
          </p>
          <p class="flex items-center gap-2 text-yellow-500 font-bold text-lg animate-bounce">
            <span>⭐</span><%= home.Rating %>
          </p>
        </div>
        <div class="flex flex-row gap-4 mt-2">
          <a 
            href="/favorites/<%= home._id %>" 
            class="bg-gradient-to-r from-pink-500 to-yellow-400 text-white rounded-full px-5 py-2 shadow-lg hover:scale-110 hover:from-yellow-400 hover:to-pink-500 transition-all duration-300 focus:outline-none focus:ring-2 focus:ring-pink-400 flex items-center gap-2"
            title="Add to Favorites"
            type="button"
          >
            <span class="text-2xl">🤍</span>
            <span class="font-semibold hidden sm:inline">Favorite</span>
          </a>
          <a 
            href="/bookings/<%= home._id %>" 
            class="bg-gradient-to-r from-cyan-500 to-purple-500 text-white rounded-full px-5 py-2 shadow-lg hover:scale-110 hover:from-purple-500 hover:to-cyan-500 transition-all duration-300 focus:outline-none focus:ring-2 focus:ring-cyan-400 flex items-center gap-2"
            title="Book this Home"
          >
            <span class="text-2xl">🛏️</span>
            <span class="font-semibold hidden sm:inline">Book Now</span>
          </a>
        </div>
      </div>
    </div>
  </div>
</body>
</html>
