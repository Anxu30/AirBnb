<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Page Not Found</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="min-h-screen bg-gradient-to-br from-cyan-700 via-purple-700 to-pink-500 flex flex-col">
  <div>
    <%- include('./partials/nav') %>
  </div>
  <div class="flex-grow flex items-center justify-center px-4">
    <div class="relative bg-white/80 backdrop-blur-md rounded-3xl shadow-2xl p-10 max-w-lg w-full flex flex-col items-center animate-fade-in-up">
      <div class="flex justify-center mb-6">
        <svg class="w-20 h-20 text-pink-400 animate-bounce" fill="none" stroke="currentColor" stroke-width="2.5" viewBox="0 0 24 24">
          <circle cx="12" cy="12" r="10" stroke="currentColor" stroke-width="2.5" fill="none"/>
          <path stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" d="M15 9l-6 6M9 9l6 6"/>
        </svg>
      </div>
      <h1 class="text-5xl font-extrabold text-transparent bg-clip-text bg-gradient-to-r from-pink-500 via-yellow-400 to-purple-600 mb-4 animate-pulse drop-shadow-lg">
        404
      </h1>
      <p class="text-xl text-gray-700 mb-6 font-semibold">
        Oops! The page you are looking for could not be found.
      </p>
      <a href="/homes" class="mt-4 inline-block bg-gradient-to-r from-cyan-500 to-pink-500 text-white rounded-full px-8 py-3 shadow-lg hover:scale-110 hover:from-pink-500 hover:to-cyan-500 transition-all duration-300 focus:outline-none focus:ring-2 focus:ring-pink-400 font-bold text-lg">
        ⬅️ Go Back Home
      </a>
    </div>
  </div>
  <style>
    @keyframes fade-in-up {
      0% {
        opacity: 0;
        transform: translateY(40px);
      }
      100% {
        opacity: 1;
        transform: translateY(0);
      }
    }
    .animate-fade-in-up {
      animation: fade-in-up 0.8s cubic-bezier(0.4, 0, 0.2, 1) both;
    }
  </style>
</body>
</html>
