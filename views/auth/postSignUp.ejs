<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Sign Up To AirBnb</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <style>
    @keyframes fadeInUp {
      0% { opacity: 0; transform: translateY(40px);}
      100% { opacity: 1; transform: translateY(0);}
    }
    .animate-fade-in-up {
      animation: fadeInUp 1.2s cubic-bezier(0.4,0,0.2,1) both;
    }
    .glass {
      background: rgba(255,255,255,0.18);
      backdrop-filter: blur(10px);
      border-radius: 1.5rem;
      box-shadow: 0 8px 32px 0 rgba(31, 38, 135, 0.25);
      border: 1px solid rgba(255,255,255,0.18);
    }
  </style>
</head>
<body class="min-h-screen bg-gradient-to-br from-cyan-700 via-purple-700 to-pink-500 flex flex-col">
  <div>
    <%-include('../partials/nav.ejs')%>
  </div>
  <div class="flex flex-col items-center justify-center min-h-[80vh] px-4">
    <div class="glass p-10 md:p-14 flex flex-col items-center gap-8 w-full max-w-md mt-10 shadow-2xl animate-fade-in-up">
      <h1 class="text-4xl md:text-5xl font-extrabold text-transparent bg-clip-text bg-gradient-to-r from-yellow-400 via-pink-500 to-purple-600 animate-pulse drop-shadow-lg text-center mb-2">
        Welcome, <%= fullName %>! 🎉
      </h1>
      <p class="text-lg text-gray-100 text-center mb-4">
        Your account has been <span class="font-bold text-yellow-300">created successfully</span>.
      </p>
      <a 
        href="/login"
        class="mt-4 inline-block px-8 py-3 rounded-full bg-gradient-to-r from-yellow-400 via-pink-500 to-purple-600 text-white font-bold text-lg shadow-lg hover:scale-105 hover:shadow-2xl transition-all duration-300"
      >
        Log In
      </a>
    </div>
  </div>
</body>
</html>
